<div class="jumbotron">
     <h1>Welcome to omhub</h1>
     
    <% if user_signed_in? %>
    	<p>
    		<%= link_to "New Project", new_project_path, class: "btn btn-default btn-lg" %>
    	</p>
    	<p>
    		or
    	</p>	
     	<p>
     		<%= link_to "Browse Projects", projects_path, class: "btn btn-primary btn-lg" %>
     	</p>
    <% else %>
      <p>
       <%= link_to "Create new Account", new_user_registration_path, class: "btn btn-primary btn-lg" %> or <%= link_to "Sign In", new_user_session_path, class: "btn btn-lg btn-primary" %>
  		</p>
  	<% end %>
</div>